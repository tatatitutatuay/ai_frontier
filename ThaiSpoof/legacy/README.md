# Legacy Scripts

This folder keeps the original one-off scripts and older experiment files for reference.

The active, configurable pipeline is in `ThaiSpoof/project/` and should be preferred for new runs:

```bash
python -m ThaiSpoof.project.run_experiment --preset smoke --stage all
```
